# 前端使用说明

## 方式一：使用纯 HTML 版本（推荐，无需 Node.js）

1. **确保后端已启动**
   - 确保 SpringBoot 后端运行在 `http://localhost:8080`

2. **直接打开 HTML 文件**
   - 双击打开 `frontend/index-standalone.html` 文件
   - 或者在浏览器中直接打开该文件

3. **开始使用**
   - 界面会自动加载学生列表
   - 点击"添加学生"按钮可以添加新学生
   - 点击"编辑"可以修改学生信息
   - 点击"删除"可以删除学生

## 方式二：使用 React 版本（需要 Node.js）

### 前提条件
需要先安装 Node.js（建议 v16 或更高版本）
- 下载地址：https://nodejs.org/

### 安装和运行步骤

1. **安装依赖**
   ```bash
   cd frontend
   npm install
   ```

2. **启动开发服务器**
   ```bash
   npm run dev
   ```

3. **访问应用**
   - 在浏览器中打开 http://localhost:3000

### 如果遇到端口占用问题

如果 3000 端口被占用，可以修改 `vite.config.js` 中的端口号：
```javascript
server: {
  port: 3001, // 改为其他端口
  ...
}
```

## 后端配置

确保后端 SpringBoot 应用：
1. 运行在 `http://localhost:8080`
2. 已添加 CORS 配置（已完成）
3. 数据库已配置并运行

## 常见问题

### 无法连接后端
- 检查后端是否正在运行
- 检查后端端口是否为 8080
- 检查防火墙设置

### 跨域错误
- 后端已经添加了 `@CrossOrigin(origins = "*")` 配置
- 如果仍有问题，检查后端是否已重新编译

